#include "../inc/vector.h"
#include "../inc/utility.h"

#include <iostream>
#include <vector>

int main() {

	/*
	ostd::vector<int> v;
	v.emplace_back(1);

	for (int i = 0; i != v.size(); ++i) {
		std::cout << v[i] << "\n";
	}
	*/

	return 0;
}
